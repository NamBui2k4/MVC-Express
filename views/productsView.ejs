<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Danh sách sản phẩm</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css"> <!-- đường dẫn của /css và /js trong boostrap đã được public trong app.js nên -->
    <script src="/js/bootstrap.bundle.min.js"></script> <!-- chỉ cần nhập tương đối thế này là  đủ-->
    <style>
        .shadow-text {
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
        }
    </style>
</head>

<body>
    <div class="container d-flex flex-column justify-content-center gap-3 vh-100 shadow-text">
        <h2 class="text-primary"> Danh sách sản phẩm </h2>
        <div form-group>
            <a href="/products/add" class="btn btn-success"> Thêm sản phẩm</a>
        </div>
        <div> Chọn một sản phẩm để xem chi tiết </div>
        <div>
            <table class="table table-light border border-dark">
                <thead class="thead-light">
                    <tr>
                        <th>STT</th>
                        <th>Tên sản phẩm</th>
                        <th>Giá</th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (products.length !==0 && products) { %>
                        <% products.forEach((element, index)=> { %>
                            <tr>
                                <td>
                                    <%= index + 1 %>
                                </td>
                                <td>
                                    <%= element.name %>
                                </td>
                                <td>
                                    <%= element.price %>
                                </td>
                                <td>
                                    <div class="d-flex gap-2 justify-content-space-between">
                                        <a class="text-success">Chỉnh sửa</a>
                                        <div> | </div>
                                        <a class="text-success"> Xóa</a>
                                    </div>
                                </td>
                            </tr>
                            <% }); %>
                                <% } else{ %>
                                    <tr>
                                        <td colspan="4" class="text-center">
                                            Không có sản phẩm nào
                                        </td>
                                    </tr>
                                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>